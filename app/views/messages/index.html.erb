<h1>Chat</h1>

<ul id="chat">
  <% @messages.each do |m| %>
  <li><%= m.content %></li>
  <% end %> 

  <%= render @messages %>

</ul>

<%= form_for Message.new, remote: true do |f| %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>

<%= subscribe_to "/messages/new" %>